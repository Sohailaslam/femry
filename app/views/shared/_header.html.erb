<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to current_user.present? ? current_user.is_admin? ? admin_root_path  : authenticated_root_path : root_path do %>
			<%= image_tag 'logo.png', alt: '', class: 'logo-footer logo-header mr-3' %>
		<% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
    	<% if current_user.present? %>
      	<li class="nav-item <%= params[:controller] == 'todos' && params[:action] == 'index' ? 'active' : '' %>">
        	<a class="nav-link" href="<%= authenticated_root_path %>">Your tasks <span class="sr-only">(current)</span></a>
      	</li>
      	<li class="nav-item <%= params[:controller] == 'users' && params[:action] == 'stats' ? 'active' : '' %>">
        	<a class="nav-link" href="<%= user_stats_path(current_user.id) %>">Stats</a>
      	</li>
      <%  end %>
			<li class="nav-item <%= params[:controller] == 'home' && params[:action] == 'public_feed' ? 'active' : '' %>">
        <a class="nav-link" href="<%= home_public_feed_path %>">Explore</a>
      </li>      
    </ul>
    <div class="form-inline my-2 my-lg-0">

      <% if current_user.present? %>
      	<ul class="navbar-nav mr-auto drop-setting">
			    <li class="nav-item dropdown">
			      <a class="nav-link dropdown-toggle btn btn-sm btn-outline-dark profile-image" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
			      	<%= show_avatar(current_user) %>
			      </a>
			      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
			      	<div class="space-left">
			      		<a href="#" class="btn btn-secondary btn-sm mr-2">FREE PLAN</a>
			      		<a href="#">Upgrade</a>
			      	</div>
			      	<div class="dropdown-divider mt-3"></div>
			      	<div class="task-details space-left">
			      		<div class="count-number">
			      			<div class="float-left">
			      				Task <i class="fa fa-exclamation-circle" data-toggle="tooltip" data-placement="bottom" title="Number of task included in your plan for this billing period"></i>
			      			</div>
			      			<div class="float-right">
			      				<%= current_user.tasks.active_tasks.count %>
			      				/
			      				100
			      			</div>
			      		</div>
			      	</div>
			      	<div class="space-left width-100">
			      	<div class="progress" style="height: 5px;">
							  <div class="progress-bar" role="progressbar" style="width: <%= current_user.tasks.active_tasks.count %>%;" aria-valuenow="<%= current_user.tasks.active_tasks.count %>" aria-valuemin="0" aria-valuemax="100"></div>
							</div>
							</div>
			      	<div class="space-left pt-2">
			      		<small>
			      			Monthly usage resets in
			      			<%= distance_of_time_in_words(current_user.created_at, DateTime.now) %>
			      		</small>
			      	</div>
			      	<div class="dropdown-divider mt-2 mb-0"></div>
			      	<%= link_to edit_user_registration_path, class: 'dropdown-item' do %>
			      		<i class="fa fa-gear mr-2"></i>Setting
			      	<% end %>

			      	<%= link_to destroy_user_session_path, class: 'dropdown-item', method: :delete do %>
			        	<i class="fa fa-power-off mr-2"></i>Log out
			        <% end %>
			      </div>
			    </li>
			  </ul>
			<% else %>
				<%= link_to 'Sign in', new_user_session_path, class: 'btn btn-success btn-sm mr-1' %>
				<%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-primary btn-sm' %>
			<% end %>
    </div>
  </div>
</nav>
